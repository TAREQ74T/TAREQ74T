# EXIT_REPORT_BC-010b — 3+3 لوحات لونية + صفحة معاينة تفاعلية

**العقد:** BC-010 — إعادة هيكلة UI الكبرى — **المرحلة 2** (تنفيذي).
**المنفذ:** MonkeyCode — منهجية وَثِيق.
**المهارة المفعّلة قبل التقرير:** `verification-before-completion`.
**المهارة المفعّلة في المرحلة:** `ui-ux-pro-max`.
**التاريخ:** 2026-09-17.
**مراجعة المستند:** v1.1 (تشمل منجز 2b).
**الإصدار:** `1.1.0` (دون تغيير).

---

## نطاق المرحلة 2

- 3 لوحات للأذكار (الأساس بني/كريمي + بديلان) و3 لوحات للصلاة (الأساس نيلي/أزرق + بديلان)، كلها من مكتبة `ui-ux-pro-max` بأسمائها الأصلية ورموزها — بلا اختراع أي لون.
- صفحة معاينة تفاعلية مؤقتة `#/palette-preview`: تبديل [الأذكار]↔[الصلاة]، [A][B][C]، [نهاري]↔[ليلي]، على الشاشة الحقيقية (بطاقات + خط + أزرار + BottomNav)، وليست mockup.
- 12 لقطة دليل + تقرير اللوحات.

## اللوحات الست (من `ui-ux-pro-max` / `data/colors.csv`)

| القسم | السلوت | الاسم الأصلي في المكتبة | الرمز | اتجاه المكتبة |
|-------|--------|--------------------------|-------|----------------|
| الأذكار | A (أساسية) | Bakery/Cafe | `#63` | Warm brown + cream white |
| الأذكار | B (بديلة) | Veterinary Clinic | `#61` | Caring teal + warm orange |
| الأذكار | C (بديلة) | Church/Religious Organization | `#74` | Spiritual purple + warm gold |
| الصلاة | A (أساسية) | B2B Service | `#5` | Professional navy + blue CTA |
| الصلاة | B (بديلة) | Alarm & World Clock | `#117` | Time amber + night indigo on dark |
| الصلاة | C (بديلة) | Sleep Tracker | `#142` | Night indigo + dream violet on dark |

- الأساسان (Bakery/Cafe و B2B Service) معتمدان من طارق.
- البديلان B وC لكل قسم أُعيد اختيارهما بمعيار طارق: اتجاه مختلف تمامًا عن الأساسية، ومختلفان عن بعضهما (تركوازي مقابل بنفسجي؛ عنبري مقابل نيلي/بنفسجي). التفاصيل في `docs/PalettesBC010.md`.

## اشتقاق الوضع الليلي

المكتبة تعطي كل صف وضعًا واحدًا؛ الوضع الليلي اشتقاق **toned** من نفس اللوحة (لا ألوان مقلوبة، ولا لوحة ثانية) وفق قاعدتَي المكتبة: `color-dark-mode` + `dark-mode-pairing`. أُنتجت قيم النهاري/الليلي عبر خلط ألوان اللوحة نفسها بالأبيض/الأسود لضمان التباين، والملف `src/data/palettes.ts` يحمل القيم النهائية صريحة. لا مزج بين لوحتين.

---

## مراجعة 2b (ما بعد مراجعة Claude)

**1. إعادة تسمية `gold` → `accent`.**
التوكن المسمّى `gold` في `palettes.ts` هو لون تمييز القسم وليس ذهبيًا حرفيًا، فأُعيدت تسميته إلى `accent` في `PaletteTokens` وفي اللوحات الست. في `PalettePreviewPage.tsx` صار يُمرَّر `--section-accent: tokens.accent` بدل تجاوز `--gold`، وصار مفتاح الأسطورة «accent القسم». لم يوجد `--pv-gold` في `index.css` (لا شيء ليعاد تسميته). **`--gold` في `:root` (عنبري إسلامي ثابت للزخارف) لم يُلمس.** القيم اللونية نفسها لم تتغير.

**2. tint ليلي لصلاة B و C على `paper` و`card` فقط.**
كان سطح الليل للوحتين B و C هو الرمادي المحايد `#0F172A`/`#192134` (بلا هوية). أُضيف tint خفيف بالخلط `mix(a,b,t) = a*(1-t) + b*t` على قنوات RGB:
- **B** (`Alarm & World Clock`): `paper = mix(#0F172A, #6366F1, 0.10) = #171F3E`، `card = mix(#192134, #6366F1, 0.12) = #22294B`.
- **C** (`Sleep Tracker`): `paper = mix(#0F172A, #7C3AED, 0.12) = #1C1B41`، `card = mix(#192134, #7C3AED, 0.14) = #27254E`.

لم يُمَس `ink`/`muted`/`border`/`strong`/`primary`/`soft`/`accent`، ولم يُمَس الوضع النهاري ولا اللوحات الأربع الأخرى.

**3. قرار مقصود: `data-theme='light'` أثناء المعاينة.**
صفحة المعاينة تُثبّت `document.documentElement.dataset.theme = 'light'` عند التركيب وتستعيد القيمة السابقة عند الإزالة. هذا **قرار مقصود لا انحراف**: المعاينة تتحكم بالألوان عبر متغيّرات نطاقها المحلي، وتثبيت وضع الجذر يمنع تعارض نظامَي التلوين (`data-theme='dark'` في الجذر vs وضع المعاينة المختار) فيبقى تبديل [نهاري]↔[ليلي] محصورًا في اللوحة المعروضة. خارج المعاينة لا يتغير سلوك الوضع الليلي.

**4. توسيع الفحص #5 ليشمل الصلاة.**
فحص تمييز الأسطح صار يشمل القسمين (3 أسطح مميزة لكل قسم × وضع)، وأُضيف فحص أن سطح الليل لصلاة B/C مُضوّى (tint) وخارج الرمادي المحايد `#0F172A` → **8/8 PASS**.

---

## الملفات

**جديدة:**
- `src/data/palettes.ts` — تعريف اللوحات الست (tokens نهاري/ليلي + الاسم الأصلي + الرمز + السبب).
- `src/pages/PalettePreviewPage.tsx` — صفحة المعاينة التفاعلية.
- `scripts/verify/bc010b-palettes.verify.mjs` — مدقّق المرحلة (6 فحوص + 12 لقطة).
- `docs/PalettesBC010.md` — تقرير اللوحات.

**معدَّلة:**
- `src/App.tsx` — route `#/palette-preview` (return مبكر بلا header/splash/BottomNav مكرر).
- `src/index.css` — أنماط المعاينة + إعادة توجيه أسطح إلى متغيّرات بـfallback + التبويب النشط بخلفية.

---

## الانحرافات الموثّقة

1. **إعادة توجيه الأسطح المثبّتة إلى متغيّرات بـfallback.**
   استُبدلت القيم المثبّتة (`#ffffff`, `#e4dfd2`, `#7a7568`, `#d8d3c5`, `#9a9589`) بـ`var(--surface, <نفس القيمة>)` وأخواتها. خارج المعاينة لا يوجد `--surface` فتعود القيمة الافتراضية نفسها → مخرجات التطبيق مطابقة حرفيًا (والانحدارات تؤكد ذلك). داخل المعاينة تُمرَّر أسطح اللوحة فتُكتسب الهوية دون mockup. (ملف `src/index.css`.)

2. **التبويب النشط في BottomNav — خلفية + لون + وزن.**
   أُضيفت خلفية للتبويب النشط في القاعدة العامة (`var(--green-100)`، وفي الليلي `rgba(255,255,255,0.06)`) استجابةً لملاحظة طارق في المرحلة 1. أدى هذا إلى تحديث لقطات أدلة المرحلة 1 عند تشغيل مدقّق `bc010a` كفحص انحدار، فأُعيدت أدلة المرحلة 1 إلى حالتها الملتزمة (`docs/evidence/BC-010/nav/`) حفاظًا على قفل أدلة المرحلة 1؛ والدليل على النمط الجديد موجود في لقطات المرحلة 2 (`bottom-nav` بلون اللوحة وخلفية).

3. **حدود النطاق:** لم يُطبَّق أي لون على التطبيق الفعلي (خارج المعاينة)، ولم تُحسم أي لوحة — القرار البصري لطارق.

---

## نتائج الفحوص (fresh — هذا التشغيل)

| الفحص | النتيجة |
|-------|---------|
| `npm run build` | EXIT=0 |
| `npm run lint` | 3 تحذيرات قديمة فقط — صفر جديد |
| `bc010b-palettes.verify.mjs` | **8/8 PASS** |
| `bc010a-nav.verify.mjs` | 8/8 |
| `bc002-search` | 9/9 |
| `bc003-prayers` | 12/12 |
| `bc004-qibla` | 8/8 |
| `bc007-visual-polish` | 15/15 |
| `bc008-adhkar` | 29/29 |
| `bc009-layout` | 16/16 |
| `dark-mode` | 5/5 |
| `sidebar-ratio` | 1/1 |
| `text-integrity` | 19/19 |
| `#/palette-preview` | يعمل — 6/6 تركيبات بلا أخطاء console |
| `quran_full.json` | دون أي تعديل (diff فارغ) |
| preview | 200 |
| الإصدار | `1.1.0` |

### فحوص `bc010b-palettes` الثمانية
1. `#/palette-preview` تعمل وتُظهر اسم اللوحة (`Bakery/Cafe`) ورمزها (`#63`).
2. التبديل: 6 تركيبات (قسم × لوحة) تعمل وتعرض الشاشة الحقيقية بلا خطأ.
3. أزرار التحكم: التبويب النشط يتميّز بخلفية + لون + وزن (والخامل بلا خلفية وبوزن عادي).
4. BottomNav مدمج داخل المعاينة (`position:fixed`) والتبويب النشط بخلفية + لون + وزن.
5. الأذكار: سطح مختلف فعليًا لكل من A/B/C في الوضعين.
6. الصلاة: سطح مختلف فعليًا لكل من A/B/C في الوضعين.
7. الصلاة ليلي: سطح B/C مُضوّى (tint) خارج الرمادي المحايد `#0F172A`.
8. نهاري ≠ ليلي (تبديل فعلي) + صفر أخطاء console.

---

## الأدلة

- **12 لقطة** في `docs/evidence/BC-010/palettes/`:
  `{adhkar,prayer}-{light,dark}-{A,B,C}.png` (3 لوحات × 2 قسم × 2 وضع).
- **نتيجة JSON:** `docs/evidence/BC-010/palettes/results/bc010b-palettes.json`.
- **انحدارات:** `docs/evidence/BC-010/palettes/regression/` (10 نتائج + لقطات).
- تم فحص اللقطات الاثنتي عشرة بصريًا، وأُصلح خلل تباين الليلي (صفوف/عناصر بيضاء مثبّتة) قبل الالتقاط — صفر عناصر منخفضة التباين.

---

## بوابات القبول (المرحلة 2)

- [x] 6 لوحات من `ui-ux-pro-max` موثّقة بأسمائها ورموزها.
- [x] `#/palette-preview` يعمل بتبديل فوري [قسم/لوحة/وضع].
- [x] الشاشة الحقيقية (AdhkarScreen + PrayerTimesPanel + BottomNav) لا mockup.
- [x] التبويب النشط بخلفية + لون + وزن.
- [x] 12 لقطة في `docs/evidence/BC-010/palettes/`.
- [x] تقرير اللوحات `docs/PalettesBC010.md`.
- [x] الانحدارات bc002..bc009 خضراء، و`bc010a` 8/8.
- [x] `quran_full.json` دون تعديل، الإصدار `1.1.0`.

---

## Commit

```
WATHEEQ_BC-010b.1: rename gold→accent + prayer B/C dark tint + verify prayer
```

---

## ⚠️ توقف إلزامي

المرحلة 2 مكتملة. **لا تبدأ المرحلة 3 قبل قرار طارق البصري.** لم تُحسم أي لوحة، ولم يُطبَّق أي لون على التطبيق الفعلي.
