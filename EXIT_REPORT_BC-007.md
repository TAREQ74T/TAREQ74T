# EXIT_REPORT_BC-007

## الحالة

- BC-007: مكتمل — Polish بصري محدود (4 نقاط).
- الإصدار: 1.1.0 (ثابت — لم يُلمس package.json).
- لا تعديل وظيفي ولا قرآني.
- المرحلة 3 (APK): لم تبدأ — بانتظار إرشادات طارق.

---

## ملخص المراحل الثلاث

### المرحلة 1 — قائمة السور بسطر واحد + زر الإعدادات

- كل سورة سطر واحد: الرقم + الاسم العربي فقط.
- أُزيل عرض الاسم الإنجليزي من القائمة فقط (يبقى في البيانات والبحث).
- أُلغي التكديس العمودي `.surah-names` وحُذفت قواعد `.surah-name-en` ونسختها الليلية.
- شبكة أمان `ellipsis` (nowrap + overflow hidden) — لا اقتطاع فعلي في النطاق الطبيعي.
- زر الترس: تحسين وضوح CSS فقط (خلفية `var(--green-100)` + حدود `var(--green-700)`
  + أبعاد أوضح)، متأقلم تلقائيًا مع الوضعين.

### المرحلة 2 — Splash + فاصل About

- Splash: خلفية متدرجة هادئة (أخضر داكن ← ذهبي خافت) بطبقتين radial + linear،
  مع `background-color` ثابت احتياطي للمتصفحات القديمة.
- زخرفة CSS حول الأيقونة (حلقتان عبر pseudo-elements) مع `pointer-events: none`.
- لا GIF؛ النصوص ورقم الإصدار كما هما؛ التخطي بالنقر والاختفاء التلقائي يعملان.
- فاصل About: إعادة تلوين `.about-ornament` بتدرج (أخضر ← ذهبي ← شفاف)
  + حبيبة وسطية `.about-ornament__gem` بإطار ذهبي.
- الآيتان وسطر الإنشاء ورقم الإصدار لم يتغيرا.

### المرحلة 3 — مراجعة وتثبيت

- فحص آلي BC-007: 15/15.
- لقطات قبل/بعد في docs/evidence/BC-007.
- مراجعة diff: لا أي تغيير وظيفي أو نصي خارج النقاط الأربع.

---

## نتائج الفحص الذاتي (bc007-visual-polish)

- قائمة: 114 سورة، كلها سطر واحد `{1:114}` (عرض 1440 وعرض 390).
- لا `.surah-name-en` ولا `.surah-names` في القائمة.
- `name_en` يظهر في نتائج البحث (بحث لاتيني Al-Faatiha).
- الترس: خلفية وحدود ومسار SVG ظاهر.
- Splash: التدرج والزخرفة و pointer-events:none و لا GIF و التخطي بالنقر.
- About: الآيتان حاضِرتان، فاصل متعدد الألوان، سطر الإنشاء والإصدار كما هما.
- النتيجة: 15 passed, 0 failed.

---

## نتائج الانحدار (BC-002..BC-006)

- BC-002 (بحث + خنجرية): 9/9.
- BC-003 (هجري + صلوات): 12/12.
- BC-004 (قبلة + UTC): 8/8.
- dark-mode (BC-004/005): 5/5.
- sidebar-ratio (BC-005): 1/1.
- text-integrity (BC-002/005): 19/19.
- BC-006 (Splash + About + إشعارات): ناجح
  - Splash/About مغطاة في فحص bc007-visual-polish.
  - الإشعارات: قسم الإشعارات ومفتاحا آية اليوم/تذكير الصلاة يعملان ويُحفظان بعد reload.
  - ملاحظة البيئة: تسليم Web Notifications الفعلي غير قابل للاختبار تحت headless
    (إذن مرفوض) — قيد معروف موثق في BC-006.

### ملاحظات

- أُزيلت عيّنة `.surah-name-en` من `dark-mode.verify.mjs` لأن العنصر لم يعد جزءًا
  من الواجهة (كانت تُحذف صامتةً عبر filter).
- release-candidate.verify.mjs: غير مشغّل عمدًا — Freeze Gate مثبت على commit قديم
  (314efe3) وسيفشل بشكل متوقع بعد BC-006 (قيد معروف سابق).

---

## القياسات قبل/بعد

- أسطر النص لكل سورة: `{2:114}` ← `{1:114}`.
- ارتفاع أول صف قائمة: 69px ← 55px.
- عقد `.surah-name-en` في القائمة: 114 ← 0.

---

## انحرافات عن العقد

- لا انحرافات وظيفية.
- تضييق نطاق «استبدال أيقونة الإعدادات» إلى تحسين CSS فقط:
  الترس SVG القياسي كان موجودًا أصلًا (BC-004)، فلم تُستبدل البنية.
- تضييق نطاق «فاصل About» إلى إعادة تلوين العنصر الموجود `.about-ornament`
  بدلًا من إضافة عنصر جديد.

---

## الأدلة

- docs/evidence/BC-007/before/ — لقطات ما قبل التغيير.
- docs/evidence/BC-007/after/ — لقطات ما بعد التغيير.
- docs/evidence/BC-007/results/bc007-visual-polish.json — تقرير الفحص الآلي.
- docs/evidence/BC-007/regression/ — لقطات أدوات الانحدار.
- docs/PolishBC007.md — وصف العقد والقرارات.

---

## المرحلة 3

- APK / Capacitor: لم تبدأ — تنتظر إرشادات طارق حصرًا.
