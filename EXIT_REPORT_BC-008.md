# EXIT_REPORT_BC-008 — أذكار الصباح والمساء (مرحلة المصدر + العرض الأساسي)

**العقد:** BC-008 — نسخة العقد 1.2 (معتمدة من المستشار الثاني، وبموافقة طارق على قرارات التوصيل والوصول).
**المنفذ:** MonkeyCode — منهجية وَثِيق.
**الحالة:** مكتمل — جميع الاختبارات والانحدارات خضراء.

---

## 1) المصدر المعتمد

| البند | القيمة |
|---|---|
| المستودع | `YousefAsalya/Islamic-Pro-azkar-API` |
| الترخيص | MIT — Copyright (c) 2026 Islamic Pro - Azkar API Contributors (النص مرفق حرفيًا في `docs/AdhkarBC008.md`) |
| الملف | `data/ar.json` |
| الباب المستهدف | `id = 27` — «أذكار الصباح والمساء» — 24 ذكرًا |

**سبب الاعتماد:** بنية مسطّحة نظيفة (132 بابًا؛ كل باب مصفوفة أذكار)، ترخيص MIT صريح،
حقول نصية مفردة `text` مع `count`، ولا حاجة لأي صوت.

### التثبيت (commit SHA — لا `@main`)

- **commit_sha:** `d793023e3b69c91674023f692425950c8ebbb64d`
- **download_url (مثبّت على الـSHA):**
  `https://raw.githubusercontent.com/YousefAsalya/Islamic-Pro-azkar-API/d793023e3b69c91674023f692425950c8ebbb64d/data/ar.json`

### sha256 (تجميد)

| الملف | sha256 |
|---|---|
| `src/data/adhkar-source.json` (التنزيل المثبّت، 166,566 بايت) | `7affdd9c1356f66f77057b631ac239cc41bde68f9bdc85d344a852a0a6ff6732` |
| `src/data/adhkar.json` (باب id=27 المستخرج) | `bd4d38c9dcef1a0c570b0ec3db7e282b5209f749e628e676477169f6e6bbb48f` |

---

## 2) ملخص المرحلتين

**المرحلة 0 — تثبيت المصدر وتحضيره:**
- تحديد `commit SHA` الحالي للمستودع وتنزيل `data/ar.json` منه حصرًا.
- `adhkar-source.json` = نسخة حرفية من التنزيل (مطابقة بايتًا-ببايت، وsha256 مطابق).
- فحص بنية `id=27` فعليًا.
- استخراج `id=27` إلى `adhkar.json` بحقول `id, text, count` فقط.
- توثيق `commit_sha` + `download_url` + `sha256` للنسختين + نص MIT في `docs/AdhkarBC008.md`.

**المرحلة 1 — العرض الأساسي:**
- `src/components/adhkar/AdhkarScreen.tsx`: قراءة محلية من `adhkar.json`، عرض نصي فقط.
- `src/pages/AdhkarPage.tsx`: صفحة بترويسة وزر عودة.
- بدون عدادات — بدون صوت — بدون مشاركة — بدون تخصيص.

---

## 3) قرار Phase 0 (بنية الواجهة)

- المصدر **لا يفصل** صباحًا عن مساء في بنيته؛ «أذكار الصباح والمساء» باب واحد (`id=27`) بقائمة مسطّحة من 24 ذكرًا.
- **القرار:** قائمة واحدة بعنوان **«أذكار الصباح والمساء»** — بلا تبويبات، تطبيقًا لقاعدة العقد.

---

## 4) الانحرافات

### انحراف #1 (إيجابي) — صفر U+FFFD فعليًا

- افترضت مسودة العقد وجود **6 أحرف U+FFFD** في أسماء الأبواب بناءً على تقرير الاستطلاع السابق.
- **الفحص الفعلي للملف المثبّت على `d793023e…` أثبت صفر U+FFFD**، بتحقق ثلاثي:
  1. Node: مسح متداخل لكل الحقول النصية (`category`/`text`/`transliteration`/…) → `FFFD total: 0`.
  2. Python: مسح بايت على مستوى الملف للحثلاثية `EF BF BD` → `0`.
  3. Node: فحص البايتات الخام `EF BF BD` + نجاح فك ترميز UTF-8 و`JSON.parse` → `0` وOK.
- **النتيجة:** لم يُجرَ أي تصحيح `before → after`، ولم يُخترع أي تصحيح لأحرف غير موجودة.
- `adhkar.json` كذلك خالٍ تمامًا من U+FFFD (نصًّا وبايتًا).

### انحراف #2 (شكلي) — شكل `adhkar.json`

- الملف كائن باب واحد: `{ id, category, items[] }`، ومفاتيح كل عنصر `items` هي `id, text, count` فقط —
  مطابق لمخطط `AdhkarChapter`/`AdhkarItem` في القسم (3ج) من العقد. المستبعدات (`transliteration`, `audio`, `filename`) غير موجودة نهائيًا.

### انحراف #3 (منهجي) — لقطة حالة الخطأ

- حالة الخطأ (`data-testid="adhkar-error"`) أُثبتت بأدلة بصرية عبر إبدال مؤقت لمحتوى `adhkar.json`
  إلى `items: []`، إعادة بناء، التقاط اللقطة، ثم **استرجاع الملف بايتًا-ببايت** (sha256 رجع إلى
  `bd4d38c9…`) وإعادة البناء النهائي. لا أثر لأي تعديل تجريبي في المستودع.

---

## 5) الحقول المستخرجة والمستبعدة

- مستخرج: `id`, `text`, `count` (الموجود لجميع الأذكار الـ24) + `category` كما ورد في المصدر.
- مستبعد نهائيًا: `transliteration`, `audio`, `filename` (لا صوت — لا ملفات — لا نقل حرفي).
- `count` محفوظ في البيانات ولا يُعرض في V1.
- مطابقة نصية كاملة: نصوص الـ24 في `adhkar.json` مطابقة حرفيًا لنظيرها في `adhkar-source.json` (24/24).

---

## 6) نتائج الاختبارات

### BC-008 — `scripts/verify/bc008-adhkar.verify.mjs`

**29 / 29 PASS** (`docs/evidence/BC-008/results/bc008-adhkar.json`)، ويشمل:
- تطابق sha256 للملفين + صفر U+FFFD (نصًّا وبايتًا) + 132 بابًا + `id=27` باسمه.
- غياب مفاتيح `transliteration/audio/filename` + 24 عنصرًا + مفاتيح `id/text/count` فقط + نصوص غير فارغة + تطابق حرفي.
- `quran_full.json` دون تعديل + لا fetch/http وقت التشغيل + استيراد محلي.
- DOM: زر «الأذكار» بجانب الترس، العنوان، 24 بطاقة، لا حالة خطأ، العودة، المسار المباشر `#/adhkar`، الوضع الليلي، شاشة 390px بلا تجاوز.

### الانحدارات (نحو `docs/evidence/BC-008/results/`)

| السويتة | النتيجة |
|---|---|
| BC-002 (بحث + خنجرية) | 9 / 9 |
| BC-003 (صلوات + هجري) | 12 / 12 |
| BC-004 (قبلة + UTC + ليلي) | 8 / 8 |
| dark-mode | 5 / 5 |
| sidebar-ratio | 1 / 1 |
| text-integrity | 19 / 19 |
| BC-007 (Polish بصري) | 15 / 15 |

---

## 7) الأدلة البصرية

المجلد: `docs/evidence/BC-008/screens/`

- `quran-toolbar-light.png` — زر «الأذكار» بجانب الترس (شريط السور).
- `adhkar-light.png` — قائمة الأذكار الكاملة (نهاري).
- `adhkar-dark.png` — قائمة الأذكار (ليلي).
- `adhkar-narrow.png` — شاشة ضيقة 390px.
- `error-state.png` — رسالة «تعذر تحميل الأذكار — بيانات المصدر غير صالحة» (حالة الخطأ).

النتائج الرقمية: `docs/evidence/BC-008/results/`، ولقطات الانحدار: `docs/evidence/BC-008/regression/`.

---

## 8) الإصدار والإعدادات

- **إصدار التطبيق بقي `1.1.0`** — لم يُرفع. «نسخة 1.2» تعني نسخة العقد، لا إصدار التطبيق (قرار طارق).
- لا تغيير على `package.json`.

---

## 9) التوصيل الجانبي (بموافقة طارق الصريحة)

- `src/App.tsx`: إضافة المسار `#/adhkar` (`Route = 'adhkar'` + `openAdhkar`).
- `src/pages/QuranPage.tsx`: زر «الأذكار» في `sidebar__toolbar` بجانب زر الإعدادات + الخاصية `onOpenAdhkar`.
- `src/index.css`: أنماط `.sidebar-adhkar-btn` + فجوة الـtoolbar + أنماط `.adhkar-screen`/`.adhkar-list`/`.adhkar-card` (نهاري/ليلي).

---

## 10) نزاهة وضوابط

- `quran_full.json`:** دون أي تعديل** (تحقق `git diff`).
- النصوص تُقرأ من المصدر المقفل حصرًا: لا توليد من الذاكرة، لا كتابة يدوية، لا تعديل على أي `text`.
- لا صوت، لا عدادات، لا تذكيرات، لا مشاركة، لا تخصيص.
- لا طلبات شبكة وقت التشغيل لجلب الأذكار (`adhkar.json` مضمّن محليًا).
- المصدر مثبّت على commit SHA؛ لا اعتماد على `main` المتحرك.

---

## 11) التوقيعات

| الطرف | التوقيع |
|---|---|
| المنفذ | MonkeyCode — منهجية وَثِيق |
| المستشار الثاني | DeepSeek |
| طارق | بانتظار التوقيع النهائي |
| المستشار الأول | بانتظار التوقيع |

---

## 12) الحالة النهائية

- لا Critical.
- جميع اختبارات القبول والانحدار ناجحة (29/29 + 7/7 سويتات).
- جاهز للاعتماد النهائي من طارق والمستشارين.
