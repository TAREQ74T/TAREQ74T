# EXIT_REPORT_BC-010c — البوصلة الجديدة + المدن (Tier 1) + منتقي المدينة

**العقد:** BC-010 — إعادة هيكلة UI الكبرى — **المرحلة 3** (تنفيذي، c.1 + c.2).
**المنفذ:** MonkeyCode — منهجية وَثِيق.
**المهارة المفعّلة قبل التقرير:** `verification-before-completion`.
**المهارة المفعّلة في المرحلة:** `ui-ux-pro-max`.
**التاريخ:** 2026-09-22.
**الإصدار:** `1.1.0` (دون تغيير).

> c.3 (تطبيق اللوحة المختارة + BottomNav) وc.4 (حذف المعاينة + MASTER_INDEX Patch 13 + التقرير النهائي) **لم تُنفَّذا بعد** — توقف مقصود بعد c.2.

---

## نطاق c.1 — البوصلة الجديدة + الاهتزاز + مفتاح الإعدادات

- إعادة بناء `QiblaCompass`: القبلة بطلة (رمز كعبة SVG + إبرة ذهبية متدرّجة)، الشمال مرجع ثانوي باهت، وردة ش/ق/ج/غ قابلة للدوران مع الجهاز، ومؤشر خارجي ثابت في الأعلى.
- مؤشر ثلاثي: `> 15°` رمادي «ليس الاتجاه»، `5–15°` أصفر «اقترب»، `≤ 5°` أخضر «الاتجاه صحيح ✓»، مع عرض رقمي للانحراف («١٠° يمينًا»).
- اهتزاز `navigator.vibrate(30)` عند **عبور واحد** إلى نطاق المحاذاة، قابل للإيقاف من الإعدادات (`haptics-toggle`)، مع حفظ في `mushaf-al-huda:haptics` (افتراضي: مفعّل).
- احتُفظ بـ`useQibla`/`useDeviceOrientation`/`useLocation`/`utils/qibla` دون لمس منطقها، وكل `data-testid` القديمة محفوظة.

## نطاق c.2 — مدن الطبقة 1 + منتقي المدينة

- بناء `src/data/cities.json` من GeoNames `cities1000` (CC BY 4.0): 4,677 سجلًا (عواصم + أكبر 1000 + كل مدن الدول العربية الـ22).
- سكربت البناء `scripts/build/build-cities.mjs` يقرأ من مصدر خارج الريبو ويكتب الملف المضغوط فقط.
- `CityPicker`: بحث مباشر عبر كل المدن (تطبيع عربي + إزالة «ال» التعريف)، منسدلة الدولة (22 عربية + الأخرى)، 50 نتيجة أولى في DOM، تحميل كسول (chunk منفصل ~478KB/135KB gzip).
- تكامل في `LocationInput` فوق «تحديد تلقائي»، مع الحفاظ على «إدخال يدوي».
- إسناد GeoNames في `AboutScreen`.

---

## نتائج الفحوص (fresh — هذا التشغيل)

| الفحص | النتيجة |
|-------|---------|
| `npm run build` | EXIT=0 |
| `npm run lint` | 3 تحذيرات قديمة فقط — صفر جديد |
| `bc010c-cities.verify.mjs` | **8/8 PASS** |
| `bc010c-compass.verify.mjs` | **7/7 PASS** |
| `bc010a-nav.verify.mjs` | 8/8 |
| `bc010b-palettes.verify.mjs` | 8/8 |
| `bc002-search` | 9/9 |
| `bc003-prayers` | 12/12 |
| `bc004-qibla` | 8/8 |
| `bc007-visual-polish` | 15/15 |
| `bc008-adhkar` | 29/29 |
| `bc009-layout` | 16/16 |
| `quran_full.json` / `adhkar.json` | دون أي تعديل (diff فارغ) |
| الإصدار | `1.1.0` |

### فحوص `bc010c-cities` الثمانية
1. `_meta` صحيح (المصدر/الترخيص/الإسناد/`sha256`/`count`).
2. عدد السجلات ≥ 4,500 (فعليًا 4,677).
3. كل سجل يحوي `n/ar/lat/lon/cc/country_ar` صالحة (0 غير مكتمل).
4. تغطية الأسماء العربية ≥ 67% (فعليًا 67.8%).
5. البحث يجد «اللاذقية» (رغم أن GeoNames يخزّنها «لاذقية» — بفضل إزالة «ال» التعريف).
6. منسدلة الدولة تعرض 22 دولة عربية.
7. اختيار مدينة يضبط `lat/lon` في `localStorage` (`mushaf-al-huda:location`).
8. صفر أخطاء console.

### فحوص `bc010c-compass` السبعة
1. البنية: قبلة + رمز كعبة + شمال مرجعي + وردة + مؤشر انحراف.
2. `> 15°` → «ليس الاتجاه» (`is-far`).
3. `5–15°` → «اقترب» (`is-near`) + «١٠° يمينًا».
4. `≤ 5°` → «الاتجاه صحيح ✓» (`is-aligned`).
5. الاهتزاز: نبضة واحدة (30) عند الوصول.
6. مفتاح الاهتزاز يبدأ مفعّلًا ثم يُطفأ ويُحفظ (`false`).
7. مع الإيقاف: الوصول لا يُطلق أي نبضة.

---

## الانحرافات الموثّقة

1. **التسعة أكواد الغائبة عن Natural Earth (`BQ, CC, CX, GF, GP, MQ, RE, SJ, YT`).**
   غير ممثّلة كيانات منفصلة في NE (مدمجة في دولها السيادية). أُخذ `country_ar` من GeoNames `alternateNamesV2` (`isolanguage='ar'`، مستوى الدولة) عبر `countryInfo.txt` → `geonameid` — **نفس المصدر المعتمد**، لا مصدر جديد. لا إسقاط لأي سجل (0 حالة ADMIN لاتيني).

2. **`TW` عبر `ISO_A2_EH`.**
   NE يحمل `ISO_A2='CN-TW'` و`ISO_A2_EH='TW'`؛ اعتُمد الرمز الثنائي الصالح (`ISO_A2_EH`) عند غياب ثنائي صالح في `ISO_A2` — قاعدة عامة لا استثناء بياني.

3. **عتبة الأسماء العربية: 70% → 67.8%.**
   السقف المرصود ضمن قواعد المصدر الصارمة = **67.8%** (3,172/4,677). رُفض: مطابقة NE بالاسم (+40 → 68.7%، خطر تصادم) والاستخراج من عمود `alternatenames` غير الموسوم (+61 → 69.1%، ممنوع). عُدّل فحص المدقّق #4 إلى **≥67%** بتوجيه طارق.

4. **سقف حجم الطبقة 1: 200KB → القياس الفعلي.**
   الحجم الفعلي **531KB خام / 132KB gzip** ضمن نطاق 400–600KB → اعتماد + توثيق إلزامي (لا حاجة لتوقف). المبرر: `quran_full.json` وحده ~1.8MB خام.

---

## الملفات

**جديدة:**
- `scripts/build/build-cities.mjs` — مولّد `cities.json` (لا مصدر خام في الريبو).
- `src/data/cities.json` — الطبقة 1 (4,677 سجلًا + `_meta`).
- `src/components/cities/CityPicker.tsx` — منتقي المدينة.
- `scripts/verify/bc010c-cities.verify.mjs` — مدقّق c.2 (8/8).
- `docs/evidence/BC-010/cities/` — 4 لقطات + النتيجة.
- `EXIT_REPORT_BC-010c.md` — هذا التقرير.

**معدَّلة:**
- `src/components/prayer-times/LocationInput.tsx` — دمج `CityPicker`.
- `src/components/about/AboutScreen.tsx` — سطر إسناد GeoNames + الرابط.
- `src/index.css` — أنماط `.city-picker`/`.city-option`.
- `docs/CityDatasetsBC010.md` — «نتيجة البناء (c.2)» + «تعديلات تشغيلية» + «قرارات مطابقة الدول».

---

## Commit

```
WATHEEQ_BC-010c.2: 3-tier cities (Tier1 GeoNames + CityPicker)
```

- BC-010c.1: 73033d5 — new compass + haptic + settings toggle
- BC-010c.0: 73511e1 — research candidate city datasets

---

## ⚠️ توقف

c.2 مكتملة. **لا c.3 ولا c.4 قبل إشارة طارق.**
