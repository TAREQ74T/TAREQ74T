# EXIT_REPORT — WATHEEQ_BC-005 v1.0

> العقد: **التحقق النهائي من المرشح 1.0 (Release Candidate) — GO**
> الحالة: **مكتمل — جاهز للتسليم**
> الفرع: `main` | قاعدة التجميد: `314efe3` | 64/64 فحصاً أخضر

---

## 1. Mode / المنهجية

- مرحلة **تحقق فقط** على المعاينة الحية (`localhost:5199`) — لا تعديل على كود المنتج.
- قيود ملزمة طُبقت: **لا تغيير** تحت `src/` أو `public/` أو `index.html` أو `package.json` منذ المرشح `314efe3`
  (تحقَّق آلياً داخل `release-candidate.verify.mjs` عبر `git diff 314efe3`).
- كل الاختبارات تعمل عبر Playwright (يُحمَّل من بيئة تحقق خارجية، لا يُضاف إلى `package.json`)
  على مخرجات البناء الحالية (PWA v1.3.0، precache 13 entries) — بلا rebuild ولا npm-run إضافية.
- النصوص مكتوبة لغة عربية مع تحويل الأرقام العربية/اللاتينية في المقارنات (`lib/harness.mjs`).

## 2. السويتات (7) — 64/64 PASS

| السويت | الملف | النتيجة | يغطّي |
|---|---|---|---|
| سلامة النص | `text-integrity.verify.mjs` | 19/19 | خط/حركات/أرقام شواردة، علامة الآية، بيانات |
| بحث BC-002 | `bc002-search.verify.mjs` | 9/9 | الألف الخنجرية، وصل، أسماء سور، نتيجة 100:1 |
| هجري + أوقات + لوحة | `bc003-prayers.verify.mjs` | 12/12 | إزاحة هجري −3..+3، خطوة دقيقة، حفظ/إعادة ضبط |
| قبلة + UTC + حصر ±30 | `bc004-qibla.verify.mjs` | 8/8 | القاهرة ≈2UTC، يدوي +3/−5، اللاذقية ≈165° (مرجع 164.87)، حصر |
| الليلي/النهاري | `dark-mode.verify.mjs` | 5/5 | تبديل/حفظ، تباين ≥4.0، عودة فاتحة |
| نسبة قائمة السور | `sidebar-ratio.verify.mjs` | 1/1 | 487/772 = 63.08% ≥ 60% |
| بوابة الإصدار | `release-candidate.verify.mjs` | 10/10 | PWA dist، أصول js/css/بيانات/خط، HTTP 200، تجميد |

نتائج كل فحص (PASS/FAIL مع المعلومة): `docs/evidence/BC-005/results/*.json`.

## 3. الأدلة واللقطات

`docs/evidence/BC-005/` — **10 لقطات** + 7 ملفات نتائج JSON:

| الملف | المضمون |
|---|---|
| `bc005-search-basit.png` | بحث «باسط» → الكهف 18:18 |
| `bc005-100-1.png` | العرض 100:1 بلا U+08Fx شاردة |
| `bc005-hijri-offset.png` | إزاحة هجري +1 (الشارة) |
| `bc005-panel-summary.png` | اللوحة مطوية مع الملخص |
| `bc005-qibla-latakia.png` | القبلة اللاذقية ≈ 165° (إبرة 164.87°) |
| `bc005-utc-manual-minus5.png` | UTC يدوي −5 |
| `bc005-step-clamp.png` | حصر الخطوة عند ±30 |
| `bc005-dark-settings.png` | مركز الإعدادات ليلي |
| `bc005-dark-quran.png` | الواجهة ليلي |
| `bc005-sidebar-ratio.png` | قياس 63.1% |
| `results/*.json` | تقارير السويتات السبع |

## 4. تعديلات أدوات التحقق (لا تمس المنتج)

- `scripts/verify/lib/harness.mjs`: محلِّل وقت يقبل دقائق غير مبطنّة (`\d{1,2}`) + `PREVIEW_URL` + كاتب تقارير JSON.
- `bc004-qibla.verify.mjs`: توحيد الأرقام العربية في مقارنات UTC؛ إعادة تحميل بعد حفظ الموقع (البوصلة تقرأ
  موقعها عند التركيب)؛ نقر «أثناء التمكين» لحلقات الحصر.
- `dark-mode.verify.mjs`: نقل دوال التباين داخل `page.evaluate`؛ التحقق بعد reload على `.settings-page`.
- `release-candidate.verify.mjs`: فحص PWA/أصول/معاينة/تجميد — بوابة GO.

## 5. نتيجة الاختبارات

| البند | النتيجة |
|---|---|
| سويتات BC-005 (7) | **64/64 PASS** — بلا أخطاء console |
| المعاينة الحية | HTTP 200 |
| تجميد `src/` منذ `314efe3` | نظيف (لا diff) |

## 6. ملاحظة استشارية (غير حاجبة — لا تعديل)

- صفوف الصلوات تعرض الدقيقة بلا بادئة صفر (الشروق «٦:٥» عوض «٦:٠٥»). تجميلي، القيمة صحيحة؛
  لم يُعدَّل التزاماً بالتجميد. إن أُريد الإصلاح لاحقاً: `formatPrayerTime` في `utils/prayer-times.ts` (حشو الدقائق).

## 7. التسليم

- المرشح 1.0 مثبَّت عند `314efe3`.
- هذا الـcommit يضيف **أدوات التحقق + الأدلة + التقريرين** فقط — بلا أي تغيير تحت `src/`.
