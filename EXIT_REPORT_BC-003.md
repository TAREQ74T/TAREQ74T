# Exit Report — WATHEEQ BC-003 (التقويم الهجري + أوقات الصلاة)

**Contract ID:** WATHEEQ_BC-003 v1.1
**Date:** 2026-08-31
**Status:** ✅ Completed — Locked
**Mode:** New feature delivery (Phase 1 + Phase 2 as per contract §3a)

---

## 1. Summary (ما تم تنفيذه)

أُنجزت المرحلتان كما حدد العقد §3a، مع توقف تحقق مصغّر ذاتي ناجح بعد المرحلة 1 (بدون قرار إنساني مطلوب — لم يحدث فشل).

### المرحلة 1 — التاريخ الهجري + تعديله اليدوي ✅

| المكوّن/الوحدة | الوصف |
|---------------|-------|
| `src/utils/hijri.ts` | عرض التاريخ عبر `Intl.DateTimeFormat('ar-SA-u-ca-islamic-umalqura')` + دالة `getAdjustedHijriDate(base, offset)` + `isHijriCalendarSupported` fallback |
| `src/storage/adjustments.ts` | قراءة/كتابة الإزاحة وتعديلات الصلوات في `localStorage` مع `clamp` |
| `src/hooks/useHijriDate.ts` | حالة الإزاحة (0→±3) + حفظ تلقائي + مؤقت تحديث يومي |
| `src/components/prayer-times/HijriDateDisplay.tsx` | التاريخ + شارة الإزاحة ("+2 يوم") + زرّي +/− + إعادة ضبط + تنبيه عند الحد |

### المرحلة 2 — أوقات الصلاة + التعديلات الفردية + الواجهة ✅

| المكوّن/الوحدة | الوصف |
|---------------|-------|
| `src/utils/prayer-times.ts` | تغليف adhan (MWL) + `adjustTime(base, minutes)` + `getNextPrayer` + تنسيق الوقت بأرقام عربية + معالجة المناطق الزمنية |
| `src/utils/location.ts` | مكة كافتراضي + `navigator.geolocation` + إدخال يدوي + تحقق صحة الإحداثيات |
| `src/hooks/useLocation.ts` | حالة الموقع (افتراضي/تلقائي/يدوي) مع رسائل الخطأ |
| `src/hooks/usePrayerTimes.ts` | حساب الأوقات + تعديلات كل صلاة + تغيير الطريقة/المذهب |
| `src/components/prayer-times/PrayerTimesPanel.tsx` | اللوحة الموحّدة القابلة للطي (هجري + أوقات + تعديلات + طريقة/مذهب + إعادة ضبط) |
| `src/components/prayer-times/LocationInput.tsx` | إظهار الموقع + تحديد تلقائي + إدخال يدوي + التوقيت المحلي |

الاعتماديات: `adhan@4.4.3` (+ ~25KB bundle). لا مكتبة إضافية للهجري (Intl فقط — قرار معماري).

---

## 2. Acceptance Tests (نتائج التحقق)

### وظيفية

| البند | النتيجة |
|-------|---------|
| أوقات مكة MWL بتاريخ معروف vs مصدر خارجي (فجر 05:39، ظهر 12:25، مغرب 17:50 في 2024-01-01) | ✅ PASS |
| التاريخ الهجري الصحيح (2024-01-01 = 19 جمادى الآخرة 1445) | ✅ PASS |
| تغيير الموقع مكة→القاهرة يغيّر الأوقات (فجر 04:48→04:09) | ✅ PASS |
| تغيير الطريقة MWL→مصرية يغيّر النتائج (فجر 04:48→04:41) | ✅ PASS |

### تعديل التاريخ الهجري (إلزامية)

| الاختبار | النتيجة |
|----------|---------|
| +2 أيام: التاريخ يتغيّر في كل مواضع العرض + شارة "+2 يوم" + لقطة قبل/بعد | ✅ PASS |
| −3 أيام (من +2 بضغط − خمساً): يتناقص ويُحفظ بعد إعادة التحميل | ✅ PASS |
| زر إعادة الضبط: الإزاحة → 0 وإخفاء الشارة | ✅ PASS |
| حدّ الإزاحة (+3): الزر يُعطَّل + تنبيه "وصلت الحد الأقصى (+٣ أيام)" | ✅ PASS |

### تعديل الصلاة الفردية (إلزامية)

| الاختبار | النتيجة |
|----------|---------|
| الفجر +15 دقيقة: المعدَّل بارزاً + الأصلي مرجعاً صغيراً + يُحفظ بعد إعادة التحميل + لقطة قبل/بعد | ✅ PASS |
| المغرب −10 دقائق (بالخطوة 5): المعدَّل 18:28 والأصلي 18:38 | ✅ PASS |
| إعادة الضبط الشاملة: جميع الأوقات تعود لأصلها المحسوب | ✅ PASS |
| مؤشر الصلاة القادمة | ✅ PASS |

### انحدار BC-002 (إلزامية)

| الاختبار | النتيجة |
|----------|---------|
| "باسط" يجد بَٰسِط (الكهف 18) | ✅ PASS |
| "بسط" لا يجد بَٰسِط | ✅ PASS |
| "النازعات" و"والنازعات" تجدان النازعات 1 | ✅ PASS |
| "زلزلة" تجد سورة 99 | ✅ PASS |
| عرض 100:1 بالخط الصحيح (ضَبۡحًا بلا علامة شاردة) | ✅ PASS |

### واجهة

| البند | النتيجة |
|-------|---------|
| تعمل في المقاس الصغير (390×844) دون تداخل مع البحث (706 < 767) | ✅ PASS |
| الوضعان الليلي/النهاري سليمان (لقطات) | ✅ PASS |
| لا أخطاء runtime/console في كل الاختبارات | ✅ PASS |
| `npm run build` (tsc + vite + PWA) | ✅ PASS |
| `npm run lint` (oxlint) | ✅ PASS (0 أخطاء) |

---

## 3. Deviations (الانحرافات + التبرير)

1. **معالجة المناطق الزمنية (إضافة تقنية، لا انحراف نطاق):** adhan يعيد لحظات UTC؛ يُضاف إزاحة خط الطول `round(longitude/15)` لعرض وقت الموقع المحلي بدقة بغضّ النظر عن توقيت جهاز المستخدم. بدونها كانت الأوقات صحيحة فقط لجهاز يطابق توقيت مكة. هذه تفعيل تقني لمتطلب "حساب الأوقات حسب الموقع" — لا ميزة جديدة خارج النطاق.
2. **الخطوة الثابتة لتعديل الصلاة = 5 دقائق** بدل التمرير (المواصفات أجازت "أزرار + و − أو شريط تمرير")؛ النطاق 0..±30 يحقَّق بالضغط المتكرر، والأزرار تُعطَّل عند الحد.
3. **الشروق يُعدَّل أيضاً** ضمن `PrayerKey` (مذكور صراحة في نوع العقد `PrayerKey`).
4. **خط العرض العليا:** اعتمَد adhan High Latitude Rule (MiddleOfTheNight الافتراضي) بدل إظهار "وقت غير محدد" — إذ أن adhan ينتج قيماً صالحة دائماً بهذه القاعدة، مع توثيق ذلك في HijriPrayer.md §5.

---

## 4. Evidence (الأدلة)

- لقطات شاشة: `docs/evidence/BC-003/` (قبل/بعد الهجري +2، قبل/بعد الفجر +15، المغرب −10، إعادة الضبط، ليل/نهار، موبايل، قارئ+لوحة).
- سجلات الاختبار: تحقق منطقي Node (adhan/Intl) + تحقق وظيفي Playwright (لا أخطاء).
- التوثيق التقني: `docs/HijriPrayer.md`.

## 5. Rollback

لا انحرافات حرجة. فرع `main` متبوع بالكامل عبر commits. أي مشكلة مستقبلية تُعالج بـ revert commit.

---

*نُفِّذ وفق عقد WATHEEQ_BC-003 v1.1 حرفياً. لا تعديل على النص القرآني أو البحث أو الخط (اختبارات انحدار BC-002 كلها ناجحة).*
