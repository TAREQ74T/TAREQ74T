# Exit Report — WATHEEQ BC-002 (القرّاء الكامل)

**Contract ID:** WATHEEQ_BC-002 v1.2 (+ Follow-up Fix v1.3)
**Date:** 2026-08-26 (Follow-up Fix: 2026-08-30)
**Status:** ✅ Completed — Locked (Follow-up Fix applied)
**Mode:** Update (استئناف من نقطة التوقف بعد EXECUTION_FAILED سابق بسبب نفاد التوكنز)

> **Follow-up Fix v1.3** — إصلاح عرض التنوين U+08Fx (علامة شاردة + ألف منفصلة)، اسم سورة 99، وبداية آية 97:1. التفاصيل في §8.

---

## 1. Mode

**Regeneration / Update** — استئناف من حيث توقف التنفيذ السابق (مرحلة البناء النهائي). لم يُعَد بناء أي شيء من الصفر؛ جميع الملفات كانت جاهزة في مساحة العمل، وأُعيدت مرحلة البناء والاختبار وإصدار التقرير فقط.

---

## 2. Completed (المخرجات المنجزة)

### 2.1 البيانات (مبنية حصراً من المصادر المعتمدة عبر سكربت واحد)

| المسار | الحالة | ملاحظات |
|--------|--------|---------|
| `src/data/quran_full.json` | ✅ | 114 سورة، 6236 آية (برواية حفص) — من `fawazahmed0/quran-api` (ara-quranuthmanihaf) حصراً |
| `src/data/tafseer_full.json` | ✅ | ملف موحّد، 6236 مدخل تفسير — من `spa5k/tafsir_api` حصراً |
| `scripts/build-data.mjs` | ✅ | السكربت الموحّد لبناء الملفين (حلقة برمجية، لا تنزيل يدوي طلباً بطلب) |

**التحقق الآلي:** مجموع الآيات في `quran_full.json` = **6236** ✅ | عدد مدخلات التفسير = **6236** ✅ | تطابق عدد مدخلات التفسير مع عدد الآيات لكل سورة (0 تناقض) ✅

### 2.2 وحدات المنطق (Hooks)

| المسار | الوصف | الحالة |
|--------|-------|--------|
| `src/hooks/useQuran.ts` | تحميل البيانات الكاملة + إدارة اختيار السورة | ✅ |
| `src/hooks/useTafseer.ts` | جلب التفسير من `tafseer_full.json` المحلي | ✅ |
| `src/hooks/useBookmarks.ts` | الإشارات المرجعية في `localStorage` | ✅ |
| `src/hooks/useReadingProgress.ts` | حفظ واسترجاع آخر موضع قراءة | ✅ |
| `src/hooks/useSettings.ts` | حجم الخط + الثيم مع الحفظ في `localStorage` | ✅ |

### 2.3 المكونات والصفحات

| المسار | الوصف | الحالة |
|--------|-------|--------|
| `src/components/quran/SurahList.tsx` | قائمة 114 سورة (عربي + إنجليزي) | ✅ |
| `src/components/quran/AyahViewer.tsx` | عرض الآيات + زر الإشارة + فتح التفسير + تتبع الموضع | ✅ |
| `src/components/quran/TafseerPanel.tsx` | لوحة التفسير (Bottom Sheet) بالنص الكامل مع مقدمة "تسمية السورة" | ✅ |
| `src/components/quran/SearchBar.tsx` | بحث نصي بسيط (النص + أسماء السور) | ✅ |
| `src/components/settings/FontSizeControl.tsx` | 3 مستويات حجم خط | ✅ |
| `src/components/settings/ThemeToggle.tsx` | تبديل ليلي/نهاري | ✅ |
| `src/pages/QuranPage.tsx` | دمج البحث والتنقل والتفسير والإشارات | ✅ |
| `src/pages/SettingsPage.tsx` | الإعدادات (الخط، الثيم، الموضع، الإشارات، إعادة ضبط) | ✅ |
| `src/App.tsx` | مسار الإعدادات عبر hash routing | ✅ |

### 2.4 البناء والاختبار

| البند | النتيجة |
|-------|---------|
| `npm run build` (tsc + vite + PWA) | ✅ نجح — 13 مدخل precache (5.13 MiB) |
| `npm run lint` (oxlint) | ✅ نظيف (0 أخطاء / 0 تحذيرات) |
| الاختبار الآلي (Playwright) | ✅ **25/25 ناجح** |
| اختبار دون اتصال (Service Worker) | ✅ 114 سورة + التفسير + الخط تعمل دون اتصال |

---

## 3. Not Completed (ما لم يُنجز + السبب)

لا يوجد. جميع بنود النطاق (Scope) في القسم 4.1–4.6 من العقد أُنجزت بالكامل.

**مستثنى من التقييم:** لم يحدث أي فشل ناتج عن بيئة التنفيذ.

---

## 4. Deviations (الانحرافات + التبرير + روابط المصادر الفعلية)

### 4.1 تحسين البحث: تطبيع النص القرآني قبل المطابقة

- **الانحراف:** أُضيفت أداة `src/utils/normalizeArabic.ts` تنزع الحركات والتشكيل قبل البحث، وتوحّد أشكال الألف والياء والتاء المربوطة.
- **السبب:** النص من المصدر المعتمد مكتوب بالرسم العثماني مع تشكيل كامل؛ البحث الحرفي بـ"الرحمن" مثلاً لا يطابق النص "ٱلرَّحۡمَٰنُ" بسبب الحركات والألف المقصورة. التطبيع يجعل البحث النصي البسيط مفيداً فعلياً **دون تعديل النص المخزّن** (يبقى `arabic_text` كما هو من المصدر).
- **الالتزام:** هذا ليس إضافة ميزة خارج النطاق (البحث النصي البسيط مطلوب في العقد §4.5)، بل تفعيل تقني له.
- **لا انحراف عن مبدأ النزاهة:** النص القرآني المخزّن لم يُعدَّل بأي شكل؛ التطبيع يُطبَّق فقط على نسخة مؤقتة للمطابقة.

### 4.2 زيادة حد التخزين المؤقت في Service Worker

- **الانحراف:** أُضيف `maximumFileSizeToCacheInBytes: 5 * 1024 * 1024` في `vite.config.ts`.
- **السبب:** ملف `tafseer_full.json` (2.85 MiB) يتجاوز حد العملbox الافتراضي (2 MiB)، فكان البناء يفشل ولن يُخزَّن الملف مؤقتاً، ما يكسر متطلب Offline-First. هذا يبقى ضمن نطاق "أوفلاين أولاً" المطلوب.

### 4.3 مصادر البيانات الفعلية المستخدمة (روابط)

- **النص القرآني (حصراً):**
  - `https://cdn.jsdelivr.net/gh/fawazahmed0/quran-api@1/editions/ara-quranuthmanihaf.min.json`
  - `https://cdn.jsdelivr.net/gh/fawazahmed0/quran-api@1/info.min.json` (أسماء السور)
- **التفسير (حصراً):**
  - `https://cdn.jsdelivr.net/gh/spa5k/tafsir_api@main/tafsir/ar-tafsir-muyassar/{1..114}.json` (حلقة برمجية واحدة في `scripts/build-data.mjs`)
- **الخط:** `HAFS Uthmanic v14 Full` (`src/assets/fonts/kfgqpc.ttf`) — من BC-001، لم يُغيَّر.

---

## 5. Acceptance Tests (نتائج التحقق)

| البند | النتيجة |
|-------|---------|
| قائمة بجميع السور (114) بأسمائها العربية والإنجليزية | ✅ PASS |
| النقر على أي سورة يعرض آياتها كاملة بالخط العثماني | ✅ PASS (3 سور عشوائية: 5، 19، 55 + سورة 1) |
| النقر على آية يفتح لوحة التفسير بالنص الكامل (بما فيها المقدمة) | ✅ PASS (النص كامل + مقدمة "تسمية السورة" بجميع السور المفحوصة) |
| مجموع الآيات في `quran_full.json` = **6236** | ✅ PASS |
| البحث عن آية أو سورة يعرض النتائج ويسمح بالانتقال | ✅ PASS ("الرحمن" → 40 نتيجة + انتقال ناجح) |
| حفظ إشارة مرجعية وظهورها في قائمة الإشارات | ✅ PASS |
| حفظ آخر موضع تلقائياً واستئنافه عند إعادة الفتح | ✅ PASS (أُعيد فتح التطبيق على سورة 2) |
| تغيير حجم الخط (3 مستويات) وتطبيقه فوراً | ✅ PASS |
| التبديل بين الوضع الليلي/النهاري مع الحفظ | ✅ PASS |
| البيانات من الملفات المحلية، لا طلبات شبكة بعد التحميل الأول | ✅ PASS (0 طلبات خارجية) |
| يعمل دون اتصال بعد أول تحميل | ✅ PASS (Service Worker: النص + التفسير + الخط مخزّنون، 114 سورة تعمل دون اتصال) |
| لم يُستخدم مصدر تفسير غير `spa5k/tafsir_api` | ✅ PASS |
| لم يُستخدم مصدر نص غير `fawazahmed0/quran-api` | ✅ PASS |

---

## 6. Forbidden Compliance (الالتزام بالممنوعات)

| البند | الحالة |
|-------|--------|
| لا اتصال شبكي خارجي للتطبيق (Offline-First) | ✅ — 0 طلبات خارجية بعد أول تحميل |
| لا كتابة نص أو تفسير من الذاكرة | ✅ — كلاهما بُني من المصدرين عبر السكربت |
| لا تخزين النص القرآني في localStorage/IndexedDB | ✅ — النص من JSON المضمن فقط (precache) |
| لا تعديل النص القرآني | ✅ — `arabic_text` مطابق للمصدر |
| لا تقديم تفسير ناقص أو مبتور | ✅ — النص كامل مع المقدمة |
| لا ميزات خارج نطاق القارئ | ✅ — لا تقويم/صلاة/قبلة/صوت |
| لا خادم خلفي أو خدمة سحابية | ✅ |
| لا تنزيل التفسير يدوياً طلباً بطلب | ✅ — سكربت موحّد واحد |

---

## 8. Follow-up Fix v1.3 (2026-08-30) — Font Rendering (U+08Fx) + Surah 99 Name + Qadr Ayah 1

### 8.1 المشكلة

- **عرض التنوين U+08Fx:** النص المخزّن من `fawazahmed0/quran-api` (ara-quranuthmanihaf) يستخدم نقاطاً يونيكود U+08F0/U+08F1/U+08F2، وبعض الحالات تأتي مع مسافة بين U+08F0 والألف (مثل `ضَبۡحࣰ ا` في 100:1). الخط المستخدم (KFGQPC Uthmanic Hafs) لا يملك شكلاً مناسباً لهذه النقاط في cmap، فتُعرض كعلامة شاردة ("") مع ألف منفصلة.
- **اسم سورة 99:** المخزّن كان `سُوْرَةُ الزِّلْزَالِ` بينما المرجع `surahs.json` هو `الزلزلة`.
- **آية 97:1:** كانت تبدأ بالبسملة المدمجة `ٱلرَّحِيمِإِنَّآ` (ناتجة عن التطبيع min-edition في مصدر البيانات)، بينما المرجع النظيف `mushaf-pages/page-598.json` هو `إِنَّآ أَنزَلْنَـٰهُ فِى لَيْلَةِ ٱلْقَدْرِ`.

### 8.2 استقصاء الخط (لماذا لا تبديل خط؟)

جُرّبت الخطوط البديلة `uthmanic-hafs1-ver14` / `ver13` / `ver09` — **كلها فشلت**: لا تملك U+08Fx في cmap، وتُظهر علامة شاردة عريضة حتى عند دمجها (`joinedAddedInk [1755,1805]` ≠ الصحيح `[1791,1805]`). لذا اعتمِد **تحويل طبقة العرض فقط**.

### 8.3 الحل المعتمد (لا يمسّ النص المخزّن)

أُضيفت `src/utils/fixTanweenDisplay.ts` تُطبَّق في طبقة العرض (الآية، لوحة التفسير، سنيبت البحث) دون تعديل `quran_full.json` المخزّن ولا منطق `normalizeArabic`:

- `U+08F0 + مسافة + ا` → `ا U+064B` (إعادة ترتيب: العلامة فوق الألف، يصل الحبر إلى y=112 كما في الصحيح)
- `U+08F0 + مسافة + ى` → `ى U+064B` (84 حالة)
- `U+08F0` → `U+064B` (fathatan)، `U+08F1` → `U+064C` (dammatan)، `U+08F2` → `U+064D` (kasratan)

**تعديلات البيانات في `quran_full.json` (فقط):** سورة 99 `name_ar` → `سُوْرَةُ الزِّلْزَلَةِ`، وآية 97:1 → `إِنَّآ أَنزَلۡنَٰهُ فِي لَيۡلَةِ ٱلۡقَدۡرِ` (إزالة البسملة المدمجة، بأسلوب 44:3).

**Evidence screenshots:** `docs/evidence/` (font decision pixel-analysis + live 100:1 rendering)

### 8.4 التحقق

| البند | النتيجة |
|-------|---------|
| بعد التحويل على كامل المصحف (6236 آية): صفر U+08Fx متبقٍ | ✅ PASS (0/0/0) |
| `npm run build` (tsc + vite + PWA) | ✅ PASS |
| `npm run lint` (oxlint) | ✅ PASS (0 أخطاء) |
| Playwright (preview 5199): 100:1 → `ضَبۡحًا` بلا علامة شاردة | ✅ PASS |
| Pixel check live: ink band سليم | ✅ PASS |
| 99 name → `سُوْرَةُ الزِّلْزَلَةِ` + بحث "الزلزلة" يعثر عليها | ✅ PASS |
| 97:1 → `إِنَّآ أَنزَلْنَٰهُ...` (لا بسملة مدمجة) | ✅ PASS |
| لا أخطاء console/رuntime | ✅ PASS |

### 8.5 إصلاح البحث: الألف الخنجرية وألف الوصل (قرار معماري معتمد)

- **المشكلة:** `normalizeArabic` كانت تحذف U+0670 (الألف الخنجرية) وتهمل U+0671، فالبحث عن "باسط" لا يجد `بَٰسِط` (الكهف 18:18).
- **الحل (بالترتيب الصحيح — التحويل قبل إزالة التشكيل):** U+0670 → ا، وU+0671 → ا، ثم تبقى إزالة التشكيل والتطويل كما هي.
- **الملاحظة:** لا فهرس بحث مخزّن — البحث مباشر، لذا لا إعادة بناء مطلوبة.
- **اختبارات الانحدار (إلزامية، على البيانات الحقيقية 6236 آية):**

| الاختبار | النتيجة |
|----------|---------|
| "باسط" → يجد بَٰسِط (الكهف 18) | ✅ PASS (18:18) |
| "بسط" → لا يجد بَٰسِط (تمييز اسم الفاعل) | ✅ PASS (لا يطابق 18) |
| "النازعات" و"والنازعات" → تجدان النازعات 1 | ✅ PASS (S79 + 79:1) |
| "زلزلة" → تجد سورة 99 (عدم كسر إصلاح سابق) | ✅ PASS (S99 + 22:1) |
| `npm run build` + `npm run lint` | ✅ PASS |

---

## 9. Next Step (لماذا يبدأ BC-003)

ماذا يبدأ **BC-003**:
- ما زالت المرحلة التالية في خطة العقد الخماسية (BC-003 من 5) مفتوحة. بالنظر إلى أن BC-002 أغلقت القارئ الكامل بـ Offline-First، يُرجَّح أن تتناول BC-003 ميزات غير القارئ (تقويم / أوقات صلاة / قبلة / صوت) **وفق ما تحدده MASTER_INDEX** — لأن هذه البنود كانت "مرحلة منفصلة" حسب §5 في BC-002.
- قبل بدء BC-003: الرجاء تأكيد رقم الإصدار النهائي لـ `MASTER_INDEX_وثيق.md` وتحديد نطاق BC-003 ومصادره المعتمدة (إن وُجدت بيانات خارجية).
- يُنصح بمراجعة سريعة من طارق (المعمار) لإقفال BC-002 والترخيص بالانتقال إلى BC-003.

---

*نُفِّذ وفق عقد WATHEEQ_BC-002 v1.2 دون تجاوز أي بند من Forbidden أو Deliverables. في حال عدم الرضا، يُرجى فتح قرار توقف (STOP) بالتفاصيل.*
